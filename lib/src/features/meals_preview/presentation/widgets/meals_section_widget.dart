import 'package:flutter/widgets.dart';

class MealsSectionWidget extends StatelessWidget {
  const MealsSectionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}