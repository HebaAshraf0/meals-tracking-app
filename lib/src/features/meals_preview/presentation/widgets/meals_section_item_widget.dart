import 'package:flutter/widgets.dart';

class MealsSectionItemWidget extends StatelessWidget {
  const MealsSectionItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}