import 'package:flutter/widgets.dart';

class MealsDatePickerWidget extends StatelessWidget {
  const MealsDatePickerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}